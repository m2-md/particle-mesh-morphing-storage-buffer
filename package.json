{
  "name": "particle-mesh-morphing-storage-buffer",
  "private": true,
  "version": "1.0.0",
  "description": "Tek TSL compute kerneli iki backend'e derleniyor: WebGPU'da var<storage, read_write>, WebGL2'de transform feedback + texelFetch. Mesh yuzeyinden alan-agirlikli ornekleme ile mesh'ten mesh'e parcacik morph'u; shader dokumu, CPU ornekleme ve GPU ms olculur",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "shaders": "vite-node tools/dumpShaders.ts",
    "bench": "vite-node tools/bench.ts",
    "format": "prettier --write ."
  },
  "dependencies": {
    "three": "0.185.1"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "@types/three": "^0.185.4",
    "@webgpu/types": "^0.1.60",
    "prettier": "^3.3.0",
    "typescript": "^5.6.0",
    "vite": "^7.0.0",
    "vite-node": "^3.0.0",
    "vitest": "^3.0.0"
  }
}
